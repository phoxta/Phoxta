import { Card, Empty } from "@/components/dash/Ui";

/** Engage - Channels area. Placeholder shell: the Channels build lands here. */
export default function ChannelsPage() {
  return (
    <Card title="Channels">
      <Empty title="Where customers reach you">SMS, WhatsApp, email and web chat are connected through Settings today; this area brings them together with per-channel capabilities.</Empty>
    </Card>
  );
}
