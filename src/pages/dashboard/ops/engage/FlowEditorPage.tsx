import { Card, Empty } from "@/components/dash/Ui";

/** Engage - Flow editor area. Placeholder shell: the Flow editor build lands here. */
export default function FlowEditorPage() {
  return (
    <Card title="Flow editor">
      <Empty title="The canvas">The flow canvas renders here.</Empty>
    </Card>
  );
}
