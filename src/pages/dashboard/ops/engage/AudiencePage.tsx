import { Card, Empty } from "@/components/dash/Ui";

/** Engage - Audience area. Placeholder shell: the Audience build lands here. */
export default function AudiencePage() {
  return (
    <Card title="Audience">
      <Empty title="Contacts, segments and consent">One view of every person - identities across channels, tags, consent, and saved segments for Journeys and Broadcasts. Contacts live in CRM today; this area is being wired up.</Empty>
    </Card>
  );
}
