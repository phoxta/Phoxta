import { DestinationSearchFormMobile } from "./experience-form";

/** The stays variant: same Where / When / Who steps, results on the map page. */
export default function StaySearchFormMobile() {
    return <DestinationSearchFormMobile searchPath="/stay-search-with-map" />;
}
