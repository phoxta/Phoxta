# Supabase (public, client-safe). Vite inlines VITE_* at build time, so these must
# be set on the Vercel project BEFORE the first production build.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: pin this deployment to one tenant instead of resolving by host.
# VITE_ORG_ID=
