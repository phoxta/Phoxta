# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr

# env (client anon key is public-safe, but keep local env out of VCS)
.env
.env.local
.env.*.local

# python (voice integration)
.venv
**/.venv
__pycache__
**/__pycache__
*.pyc

# TS incremental build info
*.tsbuildinfo

# scratch / diagnostics
diag-tmp.mjs
# Working scratch: screenshots, probe scripts, dev logs. Committed once by a
# `git add -A` from a parallel session, which is exactly why it is named here.
.rig/
scripts/.tmp-*.py

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
